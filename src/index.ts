// opensquid public entry — placeholder during Phase 0 reset.
// Task 0.6 fills this in with real module shells. Until then, this file
// exists solely so `tsc` has at least one input under the strict root
// config (TS18003 otherwise).
export {};
