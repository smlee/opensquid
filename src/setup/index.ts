/**
 * Setup: first-run wizard, pack inspector, validation report, and the
 * channel/model/secrets configuration UI surface.
 *
 * Imports from: anything in src/ (top of the dependency tree alongside mcp/).
 * Imported by: nothing in src/.
 */
export {};
