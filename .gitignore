node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.env.*.local
coverage/
.turbo/
.cache/
.idea/
.vscode/
*.tsbuildinfo

# Local runtime state — never ship internal state to public repo
.opensquid/

# Local Claude Code MCP config — contains absolute paths to local install
.mcp.json

# G.13 e2e drift-prevention report — regenerated per run via `pnpm test:e2e`
test/e2e/e2e-drift-prevention-report.md

# internal research/planning is PRIVATE — never publish (lives in loop/docs)
docs/research/
docs/tasks/

# Auto-handover docs are rebuildable on-disk projections (regenerated each handoff) — never commit
# (UCC.2: a sub-repo-launched handoff may write the projection under this repo's docs/).
docs/handover-session-*-auto.md

# fastembed model cache (downloaded artifacts)
local_cache/
package-lock.json
