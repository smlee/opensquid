# sangmin-personal — Sangmin's locked discipline as a pack.
#
# Authoritative source: docs/tasks/personal-pack.md (PERS.1 inventory at
# loop/docs/personal-pack-inventory.md). Ports the legacy
# src.legacy/pack/bundled-default/pack.yaml (313 LOC, 22 rules) to the
# new multi-file pack format.
#
# Six skill folders: git/, engine-vocab/, workflow/, versioning/,
# honesty-ledger/, phase-logging/.
#
# Ships with the npm package (see package.json `files` field) so anyone can
# load it as a starting point. Sangmin's own session points active.json at
# this pack as the universal-scope baseline; users can fork / override per
# the standard pack overlay model.
name: sangmin-personal
version: 0.1.0
scope: universal
goal: ship verified work with sangmin's locked discipline
description: |
  Personal pack converted from the legacy bundled-default pack (PERS.1).
  Encodes Sangmin's git hygiene, 7-phase workflow, PATCH-ONLY pre-1.0
  versioning, honesty-ledger claim catalog, and phase-logging discipline.
evolves: true
