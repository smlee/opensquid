# DEPRECATION STUB (DPC.6, 2026-05-30) — fully decommissioned.
#
# The 7 rules that lived in this file are now in packs/builtin/scope-architect/
# (split as 9 skills total per DPC.1+DPC.3+DPC.4+DPC.5 — the original 6
# rule kinds + recall-consumed FSM + pack-skill-authoring gate +
# pre-research-authoring gate). Opt into the built-in version by adding
# `scope-architect` to your scope's active.json packs array.
#
# This stub is a load-only no-op: when_to_load is empty so the dispatcher
# never fires the skill; rules is empty so even if forced to walk it,
# nothing runs. Validation passes (Skill schema allows rules: []).
#
# DPC.6 ships AFTER DPC.1-DPC.5 to verify behavioral equivalence is
# real, not just spec'd. Backup at:
#   ~/.opensquid/packs/sangmin-personal-rules.dpc6-backup
#
# Per [[project_opensquid_personal_rules_are_user_codex]]: user-pack discipline
# stays in user pack. The discipline pipeline graduates to opensquid built-in
# because it's universal infrastructure; personal lessons + style remain in
# this user pack's other 18 skills.
name: scope-decomposer
load: lazy
when_to_load: []
triggers:
  - kind: tool_call
rules: []
prose: |
  Deprecation stub. The discipline pipeline lives at
  packs/builtin/scope-architect/ in opensquid 0.5.216+. Add scope-architect
  to your scope's active.json to load it. This stub will be removed
  entirely in a future cleanup once active.json migrations have rolled
  out everywhere; until then it remains as a load-only no-op so any
  reference (chain-handoff directive emitting next_action.skill:
  scope-decomposer, etc.) resolves without error.
