# Test fixture pack (SD.8) — loads the scope-decomposer skill for the behavior
# test. NOT shipped, NOT active anywhere. The live deployment of the skill is the
# user's personal pack (~/.opensquid/packs/sangmin-personal-rules/); this is a
# source-controlled copy so CI can exercise the rules (the personal pack is not
# in CI, and builtin deliberately excludes the skill per personal≠shipping).
# Keep skills/scope-decomposer/skill.yaml in sync with the personal-pack copy.
name: scope-decomposer-fixture
version: 0.0.0
scope: universal
goal: "Test fixture: exercise the scope-decomposer skill's two rules (SD.8)."
