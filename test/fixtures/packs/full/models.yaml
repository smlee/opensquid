# Side-file fixture — PR-followup made `models.yaml` a real consumer of
# `loadPack` (folded into `Pack.models`). The file MUST schema-validate now.
# Shape matches `src/packs/schemas/models.ts` (ModelsConfig — top-level keys
# are alias names; values are ModelAlias objects).
fast_classifier:
  description: fixture classifier alias
  mode: subscription
  impl: cli
  cli: example-cli
  args: ['--print']
